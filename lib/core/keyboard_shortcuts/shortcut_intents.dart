import 'package:flutter/widgets.dart';

class OpenSectionMessenger extends Intent {
  const OpenSectionMessenger();
}

class OpenSectionCalls extends Intent {
  const OpenSectionCalls();
}

class OpenSectionAssistant extends Intent {
  const OpenSectionAssistant();
}

class OpenSectionCalendar extends Intent {
  const OpenSectionCalendar();
}

class OpenSectionWallet extends Intent {
  const OpenSectionWallet();
}

class OpenSectionAiSettings extends Intent {
  const OpenSectionAiSettings();
}

class OpenSectionSettings extends Intent {
  const OpenSectionSettings();
}

class OpenGlobalSearch extends Intent {
  const OpenGlobalSearch();
}

class CloseTopmostModal extends Intent {
  const CloseTopmostModal();
}

class CloseWindowOrPopup extends Intent {
  const CloseWindowOrPopup();
}

class OpenSettings extends Intent {
  const OpenSettings();
}

class ToggleChatInfoPanel extends Intent {
  const ToggleChatInfoPanel();
}
