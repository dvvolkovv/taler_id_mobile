import 'package:flutter/material.dart';

class MailAppPasswordsScreen extends StatelessWidget {
  const MailAppPasswordsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('TODO: App Passwords')));
  }
}
